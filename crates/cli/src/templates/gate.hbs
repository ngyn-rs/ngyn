use ngyn::prelude::*;

{{#services}}
use super::{{name}}::{{type}};
{{/services}}

#[injectable]
pub struct {{name}} {
{{#services}}
    pub {{name}}: {{type}},
{{/services}}
}

impl NgynGate for {{name}} {
    async fn can_activate(&self, cx: &mut NgynContext, res: &mut NgynResponse) -> bool {
        true
    }
}