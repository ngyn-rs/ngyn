use ngyn::prelude::*;

/// `{{name}}` route
///
/// # Description
/// This is the `{{name}}` route
///
/// # Arguments
/// * `_cx` - The context of the current request
pub fn {{name}}<'a>(_cx: &'a mut NgynContext) -> String {
    "Process {{name}}".to_string()
}
