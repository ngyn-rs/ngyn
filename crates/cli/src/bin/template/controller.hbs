use ngyn::prelude::*;

{{#services}}
use super::{{name}}::{{type}};
{{/services}}

#[controller]
pub struct {{name}} {
    {{#services}}
    {{name}}: {{type}},
    {{/services}}
}

#[routes]
impl {{name}} {
    #[get("/")]
    fn index(&self) -> String {
        "Hello, world!".to_string()
    }
}
