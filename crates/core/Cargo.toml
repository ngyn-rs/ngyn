[package]
name = "ngyn"
version = "0.0.2"
edition = "2021"
description = "Modular backend framework for web applications"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
nject = "0.3.0"
ngyn_macros = { path = "../macros", version = "0.0.1" }
ngyn_shared = { path = "../shared", version = "0.0.1" }
tide = "0.16.0"
