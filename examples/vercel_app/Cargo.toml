[package]
name = "vercel_app"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "all"
path = "api/[...all].rs"

[lib]
path = "src/lib.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
nject = "0.3.0"
ngyn = { path = "../../crates/core", version = "0.1.0", features = ["vercel"] }
tokio = { version = "1.32.0", features = ["full"] }
vercel_runtime = { version = "1.1.0" }
